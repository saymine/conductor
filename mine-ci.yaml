steps:

- name: 'gcr.io/cloud-builders/docker'
  id: 'build'
  args: ['build', '-t', 'europe-docker.pkg.dev/mine-artifacts-registry/conductor/conductor-server:$_TAG', '-f', 'docker/server/Dockerfile', '.']

- name: 'gcr.io/cloud-builders/docker'
  id: 'push'
  args: ['push', 'europe-docker.pkg.dev/mine-artifacts-registry/conductor/conductor-server:$_TAG' ]

options:
  machineType: 'N1_HIGHCPU_32'